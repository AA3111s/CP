#include <bits/stdc++.h>
using namespace std;

int main(){

    int n;
    while(n--){
        pair<int, int> p;
        cin >> p.first >> p.second;
    }
    int q;
    while(q--){
        int t;
        for( auto x : p.second){
            if(t <= p.second){
                cout << p.first;
            }
        }
    }
}