#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int a, b, x;
        cin >> a >> b >> x;

        if(a > b){
            while(a > b){
            int temp1 = b + 1;
            int temp2 = ceil((double)b / x);
            }
        }
    }
}